{
  "name": "mindmap-ai",
  "version": "0.1.3",
  "private": true,
  "description": "Standalone Obsidian plugin repo for the Mindmap Python-powered workflow.",
  "license": "MIT",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test $(find src -name '*.test.ts' -print)",
    "check": "npm run lint && npm run typecheck && npm test && npm run build && npm run validate",
    "validate": "node scripts/validate-release.mjs",
    "release:prepare": "npm run build && npm run validate && node scripts/prepare-release.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.6.1",
    "esbuild": "^0.25.10",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "obsidian": "latest",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.59.3"
  },
  "dependencies": {
    "motion": "^12.38.0"
  }
}
